document.addEventListener('DOMContentLoaded', function () {
    let navSpot = document.getElementById('navContent');
    if (navSpot !== null) {
        fetch("/navfragment.html").then((response) => response.text().then((html) => {
            navSpot.innerHTML = html;
        }))
    }
});